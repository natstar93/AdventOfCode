import getNoOfUniqueSegments, { getOutputs } from './getUniqueSegments';
import data from './data';

const part1Answer = getNoOfUniqueSegments(data);
const part2Answer = getOutputs(data);

export default { part1Answer, part2Answer };
