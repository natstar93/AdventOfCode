import getVisibleDots, { getFinalOutput } from './getVisibleDots';
import data from './data';

const part1Answer = getVisibleDots(data);
const part2Answer = getFinalOutput(data);

export default { part1Answer, part2Answer };
